<h1 align="center">{{ project_name }}</h1>

---

[![](https://img.shields.io/badge/fbidu's%20cookiecutter-gray?style=for-the-badge&logo=cookiecutter)](https://github.com/fbidu/cookie-py)
<!-- Insert an image/gif/code snippet of the working project -->

{% if language == "Both" %}
[🇧🇷 Descrição em Português](#uso)
{% endif %}
{% if language == "Both" or language == "EN" %}
## Usage

The requirements are

- Python >= {{ python_version }}
- [uv](https://docs.astral.sh/uv/) (recommended) or pip

1. Clone this repo
2. Run `uv sync --dev` (or `pip install -e .` if using pip)
3. 🎆

## Development

This project uses modern Python tooling:

- **[uv](https://docs.astral.sh/uv/)** for fast dependency management and virtual environments
- **[ruff](https://docs.astral.sh/ruff/)** for linting and code formatting
- **[pyright](https://github.com/microsoft/pyright)** for type checking
- **[pytest](https://docs.pytest.org/)** for testing
- **[prek](https://github.com/j178/prek)** for git hooks (drop-in `pre-commit` replacement)

### Setup

1. Install [uv](https://docs.astral.sh/uv/) and [prek](https://github.com/j178/prek) globally
2. Clone this repository
3. Run `uv sync --dev` to install dependencies
4. Run `prek install --install-hooks` to set up both pre-commit and pre-push hooks

### Commands

- `uv run pytest` - Run tests
- `uv run ruff check .` - Run linting
- `uv run ruff format .` - Format code
- `pyright` - Run type checking
- `prek run --all-files` - Run all hooks
{% if dependency_updates == "renovate" %}
### Dependency updates

This project uses [Renovate](https://docs.renovatebot.com/) to keep dependencies and pinned tool versions current. It reads `renovate.json`.
{% if ci_provider == "github" %}
Activate it with **one** of these (using both causes duplicate PRs):

- **Hosted app (recommended):** install the [Renovate GitHub App](https://github.com/apps/renovate) — free for public and private repos — then delete `.github/workflows/renovate.yml`.
- **Self-hosted:** keep the bundled `.github/workflows/renovate.yml` and add a `RENOVATE_TOKEN` repository secret (a PAT with `repo` + `workflow` scopes, or a GitHub App token).
{% elif ci_provider == "forgejo" %}
To activate it:

1. Create a `RENOVATE_TOKEN` — a Forgejo access token (Settings → Applications) with read/write on repositories. Save it as a repository (or organization) **Actions secret**.
2. The bundled `.forgejo/workflows/renovate.yml` runs weekly (and on demand via *Run workflow*). Adjust its schedule if you like.
{% else %}
Point a Renovate runner — the [hosted app](https://github.com/apps/renovate) or a scheduled CI job — at this repo and it will pick up `renovate.json`.
{% endif %}
{% endif %}
{% if ci_provider == "github" or ci_provider == "forgejo" %}
### Template updates

This project was generated from [cookie-py](https://github.com/fbidu/cookie-py) with [Copier](https://copier.readthedocs.io/), which records the template version in `.copier-answers.yml`. Pull later template changes anytime with:

```bash
copier update --trust
```

A scheduled job also checks weekly and opens a PR when the template has newer changes. To enable it:
{% if ci_provider == "github" %}
- Add a `COPIER_UPDATE_TOKEN` repository secret — a PAT with `repo` + `workflow` scopes. The `workflow` scope is required because updates can touch files under `.github/workflows/`, which the built-in `GITHUB_TOKEN` cannot push.
{% elif ci_provider == "forgejo" %}
- Add a `COPIER_UPDATE_TOKEN` **Actions secret** — a Forgejo access token (Settings → Applications) with repository read/write. The scheduled `.forgejo/workflows/copier-update.yml` opens the PR via Forgejo's AGit push, so no extra CLI is needed.
{% endif %}
{% endif %}

{% endif %}
{% if language == "Both" or language == "PT-BR" %}
## Uso

Os requerimentos são

- Python >= {{ python_version }}
- [uv](https://docs.astral.sh/uv/) (recomendado) ou pip

1. Clone esse repositório
2. Execute `uv sync --dev` (ou `pip install -e .` se usando pip)
3. 🎆

## Desenvolvimento

Este projeto usa ferramentas modernas do Python:

- **[uv](https://docs.astral.sh/uv/)** para gerenciamento rápido de dependências e ambientes virtuais
- **[ruff](https://docs.astral.sh/ruff/)** para linting e formatação de código
- **[pyright](https://github.com/microsoft/pyright)** para verificação de tipos
- **[pytest](https://docs.pytest.org/)** para testes
- **[prek](https://github.com/j178/prek)** para git hooks (substituto drop-in do `pre-commit`)

### Configuração

1. Instale [uv](https://docs.astral.sh/uv/) e [prek](https://github.com/j178/prek) globalmente
2. Clone este repositório
3. Execute `uv sync --dev` para instalar dependências
4. Execute `prek install --install-hooks` para configurar os hooks de pre-commit e pre-push

### Comandos

- `uv run pytest` - Executar testes
- `uv run ruff check .` - Executar linting
- `uv run ruff format .` - Formatar código
- `pyright` - Executar verificação de tipos
- `prek run --all-files` - Executar todos os hooks
{% if dependency_updates == "renovate" %}
### Atualização de dependências

Este projeto usa o [Renovate](https://docs.renovatebot.com/) para manter as dependências e versões fixadas de ferramentas atualizadas. Ele lê o `renovate.json`.
{% if ci_provider == "github" %}
Ative com **uma** destas opções (usar as duas gera PRs duplicados):

- **App hospedado (recomendado):** instale o [Renovate GitHub App](https://github.com/apps/renovate) — gratuito para repositórios públicos e privados — e então remova o `.github/workflows/renovate.yml`.
- **Auto-hospedado:** mantenha o `.github/workflows/renovate.yml` incluído e adicione um secret `RENOVATE_TOKEN` (um PAT com escopos `repo` + `workflow`, ou um token de GitHub App).
{% elif ci_provider == "forgejo" %}
Para ativar:

1. Crie um `RENOVATE_TOKEN` — um token de acesso do Forgejo (Settings → Applications) com leitura/escrita em repositórios. Salve-o como um **Actions secret** do repositório (ou da organização).
2. O `.forgejo/workflows/renovate.yml` incluído roda semanalmente (e sob demanda via *Run workflow*). Ajuste o agendamento se quiser.
{% else %}
Aponte um runner do Renovate — o [app hospedado](https://github.com/apps/renovate) ou um job de CI agendado — para este repositório e ele usará o `renovate.json`.
{% endif %}
{% endif %}
{% if ci_provider == "github" or ci_provider == "forgejo" %}
### Atualização do template

Este projeto foi gerado a partir do [cookie-py](https://github.com/fbidu/cookie-py) com o [Copier](https://copier.readthedocs.io/), que registra a versão do template em `.copier-answers.yml`. Traga mudanças posteriores do template a qualquer momento com:

```bash
copier update --trust
```

Um job agendado também verifica semanalmente e abre um PR quando o template tem novidades. Para ativar:
{% if ci_provider == "github" %}
- Adicione um secret `COPIER_UPDATE_TOKEN` — um PAT com escopos `repo` + `workflow`. O escopo `workflow` é necessário porque atualizações podem mexer em arquivos sob `.github/workflows/`, que o `GITHUB_TOKEN` embutido não consegue enviar.
{% elif ci_provider == "forgejo" %}
- Adicione um **Actions secret** `COPIER_UPDATE_TOKEN` — um token de acesso do Forgejo (Settings → Applications) com leitura/escrita em repositórios. O `.forgejo/workflows/copier-update.yml` agendado abre o PR via push AGit do Forgejo, sem precisar de CLI extra.
{% endif %}
{% endif %}

{% endif %}
